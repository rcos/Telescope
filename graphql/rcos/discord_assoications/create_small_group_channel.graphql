mutation CreateOneSmallGroupChannel(
    $project_id: String!,
    $channel_id: String!,
    $kind: channel_type!,
) {
    insert_channel_one(object: {
        project_id: $project_id
        channel_id: $channel_id,
        kind: $kind,
    }) {
        channel_id
    }
}