version: "3.1"

services:
  db:
    image: postgres
    restart: always
    environment:
      - POSTGRES_USER
      - POSTGRES_PASSWORD
    ports:
    - "5432:5432"
    - "5432:5432/udp"

  telescope:
    restart: "no"
    depends_on:
      - db
    build: .
    ports:
    - "80:8080"
    - "443:8443"
