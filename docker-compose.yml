version: "3.1"

services:
  db:
    image: postgres
    restart: always
    environment:
      - POSTGRES_USER
      - POSTGRES_PASSWORD
    ports:
    - "5432:5432"
    - "5432:5432/udp"
  telescope:
    restart: "no"
    network_mode: host
    depends_on:
      - db
    build: .
