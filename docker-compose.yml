version: "3.1"

services:
  db:
    image: postgres
    restart: always
    environment:
      - POSTGRES_USER
      - POSTGRES_PASSWORD
    network_mode: host
  telescope:
    restart: "no"
    network_mode: host
    depends_on:
      - db
    build: .
