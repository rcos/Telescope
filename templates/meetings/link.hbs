<a href="{{url}}" class="btn btn-{{class}} w-100 h-100">
    <div class="row">
        <div class="col-2">
            <h2>
                <i data-feather="external-link"></i>
            </h2>
        </div>
        <div class="col-8 px-0">
            {{text}} <br>
            <span class="small">
                {{domain_of url}}
            </span>
        </div>
        <div class="col-2">
            {{! Dummy div to align the text center }}
        </div>
    </div>
</a>
